## 双向绑定的原理

v-model ==> value="xx" @input/@change

ViewModel为model和view之间的桥梁：监听模型数据的改变和控制视图行为、处理用户交互。

数据更新之后，视图发生变化，这个过程是怎样的？

1. 我们用一个监听器Observer去监听data中的数据以及对象内的属性；

2. 我们用一个解析器complile去解析Vue 模板指令，将模板中的变量都替换成数据，然后初始化渲染页面视图，并将每个指令对应的节点绑定更新函数，添加监听数据的订阅者，一旦数据有变动，收到通知，调用更新函数进行数据更新。

3. 订阅者：Watcher，我们添加了订阅者之后，一旦data中订阅的数据发生变化，就会去通知解析器中的更新函数去执行更新操作，从而更新视图

4. 订阅器：Dep，用来收集订阅者并且对其进行管理。

## Vue Router 的 hash 和 history 有什么区别，实现原理

hash 模式是一种把前端路由的路径用井号 # 拼接在真实 url 后面的模式。当井号 # 后面的路径发生变化时，浏览器并不会重新发起请求，而是会触发 onhashchange 事件。

pushState() 设置的新 URL 可以是与当前 URL 同源的任意 URL；而 hash 只可修改 # 后面的部分，因此只能设置与当前 URL 同文档的 URL；

history 模式下，前端的 URL 必须和实际向后端发起请求的 URL 一致，需要服务器端做额外的配置，否则会出现刷新404的情况。

## Vue Diff算法的原理

Diff算法用于比较前后两次数据变化的差异，只更新有变化的dom。

如果旧节点和新节点是不同的标签，那么就直接进行替换；

如果是相同的标签，那么就对比是否有子节点；

然后就用首尾指针法去更新dom节点